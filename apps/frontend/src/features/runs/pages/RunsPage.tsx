import { PageHeader } from "@/shared/components/PageHeader";

export function RunsPage() {
  return (
    <section className="page">
      <PageHeader
        description="The future home for processed runs, logs, and extracted route data."
        title="Runs"
      />
    </section>
  );
}
