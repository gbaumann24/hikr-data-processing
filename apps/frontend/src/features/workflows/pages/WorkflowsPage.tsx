import { PageHeader } from "@/shared/components/PageHeader";

export function WorkflowsPage() {
  return (
    <section className="page">
      <PageHeader
        description="The future home for pipeline and agent workflow controls."
        title="Workflows"
      />
    </section>
  );
}
